import React, { ReactElement } from "react";
import { VStack } from "native-base";

const Details = (): ReactElement => {
  return <VStack flex={1} bg={"gray.800"} p={6}></VStack>;
};

export default Details;
